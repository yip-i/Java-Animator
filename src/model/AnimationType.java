package model;

/**
 * This class represents the possible animation types that could be used to create an object
 * that extends the AbstractAnimationEvent class.
 */
public enum AnimationType {
    MOVE,
    RESIZE,
    CHANGE_COLOUR
}
